#makefile that compiles command shell

all: #Don't have any files yet
    g++ <filename> -Wall -Werror -ansi -pedantic

rshell: #Still don't have any files yet
    g++ <filename> -Wall -Werror -ansi -pedantic 
