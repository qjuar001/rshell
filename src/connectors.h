#ifndef CONNECTERS_H
#define CONNECTERS_H

#include "cmdWorked.h"
#include <string>

using namespace std;

class connecters : cmdWorked
{
   protected:
      string op;
      bool runNext;
       

};

#endif
